[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "telegram-scraper"
dynamic = ["version"]
description = "Terminal tool for scraping and analysing Telegram channels, groups and chats"
requires-python = ">=3.11"
dependencies = [
    "telethon<2",
    "python-dotenv",
    "pandas",
    "numpy",
    "tqdm",
    "pyarrow",
    "openpyxl",
]

[project.optional-dependencies]
dev = ["pytest"]

[project.scripts]
telegram-scraper = "telegram_scraper.cli:main"

[tool.setuptools]
packages = ["telegram_scraper"]

[tool.setuptools.dynamic]
version = {attr = "telegram_scraper.__version__"}
